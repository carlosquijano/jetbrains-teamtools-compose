# jetbrains-teamtools-compose

Simple docker compose setup for running the JetBrains Team Tools docker images.

## Prerequisites

- Docker & Docker Compose

## Run

Run `acme.land` example

    docker-compose --project-name=acmeland up